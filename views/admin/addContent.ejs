<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>

<div class="admin-container">

    <div class="admin-header">
        <h1>Add Content</h1>
        <a href="/admin/dashboard" class="back-btn">Back</a>
    </div>

    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <form action="/admin/content/add" method="POST" enctype="multipart/form-data">

        <div class="form-group">
            <label>Class</label>
            <select name="classNumber" required>
                <option value="">Select Class</option>
                <option value="11">Class 11</option>
                <option value="12">Class 12</option>
            </select>
        </div>

        <div class="form-group">
            <label>Section</label>
            <select name="sectionSlug" required>
                <option value="">Select Section</option>
                <option value="lectures">Chapterwise Lectures</option>
                <option value="quick-revision">Quick Revision</option>
                <option value="programming-practice">Programming Practice</option>
                <option value="notes">Notes</option>
                <option value="short-notes">Short Notes</option>
            </select>
        </div>

        <div class="form-group">
            <label>Title</label>
            <input type="text" name="title" required />
        </div>

        <div class="form-group">
            <label>Description</label>
            <textarea name="description"></textarea>
        </div>

        <div class="form-group">
            <label>Content Type</label>
            <select name="contentType" required>
                <option value="">Select Type</option>
                <option value="video">Video (YouTube)</option>
                <option value="pdf">PDF (Notes)</option>
            </select>
        </div>

        <div class="form-group">
            <label>YouTube Video URL</label>
            <input type="url" name="videoUrl" placeholder="https://www.youtube.com/watch?v=..." />
        </div>

        <div class="form-group">
            <label>Upload PDF (for Notes)</label>
            <input type="file" name="file" accept="application/pdf" />
        </div>

        <div class="form-group">
            <label>Order (optional)</label>
            <input type="number" name="order" />
        </div>

        <button type="submit" class="btn">Add Content</button>

    </form>

</div>

</body>
</html>
